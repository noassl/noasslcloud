global:
  sendAnonymousUsage: true
entrypoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
    http:
      tls:
        certResolver: letsencrypt
        domains:
          - main: noassl.at
            sans:
              - "*.noassl.at"
providers:
  docker:
    exposedByDefault: false
    network: traefik
certificatesResolvers:
  letsencrypt:
    acme:
      email: jneissl27@gmail.com
      storage: /etc/traefik/cloudflare.acme.json
      dnsChallenge:
        provider: cloudflare
  hetzner:
    acme:
      email: jneissl27@gmail.com
      storage: /etc/traefik/hetzner.acme.json
      dnsChallenge:
        provider: hetzner
log:
  filePath: /logs/traefik.log
  level: ERROR
accessLog:
  filePath: /logs/access.log
api:
  dashboard: true
  insecure: true